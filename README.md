# ambar-rocketpy

RocketPy testing for Project AMBAR

Requires the latest dev build of RocketPy (master branch), as the current release build contains a bug
in the controller backend which causes finite differentation of timesteps to fail.
